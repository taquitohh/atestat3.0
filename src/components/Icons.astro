---
export interface Props {

gap: number;
size: number;
}

const { gap, size } = Astro.props;
---
<div class="iconitze" style={{gap: `${gap}px`, size:`${size}px`}}>
    <slot></slot>
</div>
<style>
.iconitze {
	display: flex;
	gap: 200px;
	justify-content: center;
	align-items: center;
}
</style>
